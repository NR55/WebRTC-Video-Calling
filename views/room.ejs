<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room</title>
    <link rel="stylesheet" href="/roomStyle.css" />
    <script>
        const ROOM_ID = "<%=roomId%>";
        const USER_NAME = "<%=username%>"
    </script>
    <script src="https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <style>
        /* Add custom tooltip */
        video[data-username]::after {
            content: attr(data-username);
            position: absolute;
            bottom: -25px;
            /* Adjust the position of the tooltip as per your layout */
            left: 50%;
            transform: translateX(-50%);
            padding: 5px;
            background-color: rgba(0, 0, 0, 0.8);
            color: #fff;
            border-radius: 5px;
            white-space: nowrap;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        video[data-username]:hover::after {
            opacity: 1;
        }
    </style>
</head>

<body>
    <h1>Username : <%=username%>
    </h1>
    <div id="video-grid"></div>
    <div class="chat-container">
        <div id="chat"></div>
        <form id="chat-form">
            <input type="text" id="chat-input" placeholder="Type your message...">
            <button type="submit">Send</button>
        </form>
    </div>
    <br><br>
    <input type="submit" class="submit-btn" onclick="leaveRoom()" value="Leave Room">
    <input type="text" id="urlCopy">
    <script>
        function leaveRoom() {
            window.location.href = "/lobby";
        }
        document.getElementById("urlCopy").value = ROOM_ID;
    </script>
</body>

</html>